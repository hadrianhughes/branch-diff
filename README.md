# branch-diff

A more ergonomic environment for reviewing code.

## Usage

```
branch-diff -f <from_branch> -t <to_branch>
```

## Build locally

1. Ensure Rust and Cargo are installed.
2. Run `cargo build`
